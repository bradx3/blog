SITE.user = "brad"
SITE.host = 'hubert.lucky-dip.net'
SITE.remote_dir = "/var/www/blog/"

task :default => :build

desc 'deploy the site to the webserver'
task :deploy => [:build, 'deploy:rsync']

# EOF
